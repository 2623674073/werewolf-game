# 定位

当前文档用于汇总 `docs` 目录中的架构与集成说明。

# 摘要

1. `architecture.md`：分层架构、对局生命周期、事件隐私和失败模型。
2. `mcp-game-historian.md`：终局史官 MCP 的 Tool 契约、分回合分析、持久化和恢复策略。
3. `model-compatibility.md`：OpenAI-compatible 能力要求与真实模型验证边界。
4. `stability.md`：4 局游戏、20 个 SSE 观察者的离线并发回归。
5. `adr/`：SSE、临时流式帧、SQLite、MCP 和 Demo Runtime 的关键取舍。
